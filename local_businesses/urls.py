from django.urls import path
from . import views

app_name = 'local_businesses'
urlpatterns = [
    # We'll add business-related paths here later
    # Example:
    # path('', views.business_list, name='business_list'),
]